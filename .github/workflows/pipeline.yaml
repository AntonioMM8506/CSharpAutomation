name: CICD

on: 
  push:
    branches: [main]

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.x

    - name: Restore
      run: dotnet restore
    
    - name: Buildº
      run: dotnet build
